import { Global } from "@emotion/react";
import * as React from "react";

const Markdown = () => (
  <Global
    // TODO: wrtie md custom styles here
    styles={`
        .markdown {} 
    `}
  />
);
export default Markdown;
